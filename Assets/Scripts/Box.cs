using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Box : MonoBehaviour {

    public Box(Vector3 position)
    {
        //instantiate a box
        //set parent to panel
        //Set position, scale and size
        //Set score to zero
    }

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
